java中的拷贝是指将一个对象的属性传递给另一个对象（从而实现复用对象的部分或全部数据）。
拷贝分为深拷贝和浅拷贝。

数据类型：分为基本数据类型和引用数据类型，前者为值传递，后者为地址传递。
浅拷贝当成员变量为基本数据类型时，只不过是把对象的值赋给了新的对象，对一个对象的修改不会影响另一个对象的值。
当成员变量为是引用类型时（如数组、某个类的对象时），会将该成员的地址复制一份给新的对象，实际上两个对象指向的是一个地址，则改变其中的值，两个对象的值都会被改变。
